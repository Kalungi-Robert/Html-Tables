Instructions

Try to recreate the following table, add your code in a private GitHub repository along with your instructor(s) as collaborator(s) and submit the repo’s URL.

The  basics of understanding tables, table rows, table data cells, tables with spans (both colspan & rowspan). 